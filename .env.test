# Test environment configuration for Bob's Brain
# Copy to .env or source before running tests locally.
# CI sets these automatically.

# Test mode - stubs external services
BOB_TEST_MODE=1

# Stub all external HTTP calls (Agent Engine, GCS, etc.)
BOB_STUB_EXTERNAL=1

# LLM mode: stub|replay|real
# stub   - Returns canned responses (fast, deterministic)
# replay - Replays recorded LLM responses (medium, deterministic)
# real   - Calls live LLM API (slow, non-deterministic, requires credentials)
BOB_LLM_MODE=stub

# OpenTelemetry - disable trace export in tests
OTEL_TRACES_EXPORTER=none

# GCP Project (used by integration tests when BOB_STUB_EXTERNAL=0)
# PROJECT_ID=your-dev-project
# LOCATION=us-central1
